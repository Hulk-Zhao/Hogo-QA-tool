# 依赖与构建产物
node_modules
dist
dist-server
dist-offline
dist-ssr
coverage
*.tsbuildinfo
.vite

# 日志
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# 编辑器 / 系统
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 本地环境变量（绝不入库）
.env
.env.*
!.env.example

# 本地密钥（真 key 探针用；绝不入库）
.secrets/
*.local

# 内部开发记录（原作者的本地笔记，不属于项目产物）
.workbuddy/

# Vite 临时文件与补丁草稿
vite.config.*.timestamp-*.mjs
_*.txt

# 真机探针的**产物**（脚本保留、结果不入库）：
# 这些 JSON/TXT 是某台机器某次运行的输出，可用 .probe/*.mjs 重新生成。
.probe/*.json
.probe/*.txt
.probe/*.jsonl
.probe/*.log
.probe/*.pdf
.probe/*.xlsx
.probe/*.png
.probe/downloads/
.probe/out/
.probe/p3-png/
.probe/*-render/
.probe/*-render-*/
.probe/p4-ab-*/
.probe/p4-one-*/
.probe/p4-xlsx-media/